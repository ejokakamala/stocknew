<div class="mx-auto md:w-2/3 w-full mt-14">
  <div class="flex justify-between">
    <h1 class="font-bold text-3xl">New flock</h1>
    <%= link_to "Back to flocks", flocks_path, class: "ml-2 rounded-lg py-2 px-4 text-white bg-gray-600 inline-block font-medium" %>
  </div>

  <%= render "form", flock: @flock %>

</div>
